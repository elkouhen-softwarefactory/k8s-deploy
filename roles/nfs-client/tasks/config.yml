- name: "Update file /etc/fstab"
  lineinfile:
    path: /etc/fstab
    regexp: "^{{ groups['nfs-master'][0] }}:/var/nfs/{{item}}/"
    line: "{{ groups['nfs-master'][0] }}:/var/nfs/{{item}}/ /nfs/{{item}}/ nfs rw 0 0"
  become: yes