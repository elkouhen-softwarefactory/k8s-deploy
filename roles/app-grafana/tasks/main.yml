- name: Copy Resources
  template:
    src: "templates/{{item}}"
    dest: "/tmp/{{item}}"
  with_items:
  - ingress.yml

- name: Create Resources
  command: "kubectl apply -f /tmp/{{item}}"
  become: yes
  with_items:
  - ingress.yml

- name: Helm List
  command: helm list
  become: yes
  become_user: root
  register: helm_list

- name: Install grafana
  command: helm install --name grafana stable/grafana
  become: yes
  when: "'grafana' not in helm_list.stdout"