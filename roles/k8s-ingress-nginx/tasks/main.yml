# LINT-ERROR
- name: "Helm List"
  command: kubectl get ns
  become: yes
  become_user: root
  register: ns_list
  changed_when: False

- name: "Create the namespace for ingress-nginx"
  command: kubectl create namespace ingress-nginx
  become: yes
  become_user: root
  when: "'ingress-nginx' not in ns_list.stdout"

- name: "Create folder /root/ingress-nginx"
  file:
    path: /root/ingress-nginx
    state: directory
    mode: 0755
  become: yes
  become_user: root

- name: "Copy kustomization.yaml"
  template:
    src: kustomization.yaml
    dest: /root/ingress-nginx/kustomization.yaml
  become: yes
  become_user: root
  when: registry is defined

- name: "Kustomize ingress-nginx"
  command: kubectl apply --kustomize /root/ingress-nginx
  become: yes
  become_user: root

#- name: "Install the cert-manager Helm chart"
#  command: /usr/local/bin/helm install --name ingress-nginx --namespace ingress-nginx --set webhook.enabled=false stable/ingress-nginx
#  become: yes
#  become_user: root
#  when: "'ingress-nginx' not in helm_list.stdout"

#- name: Copy Resources
#  template:
#    src: "templates/patch.yml"
#    dest: "/tmp/patch.yml"

# LINT-ERROR
#- name: Patch ConfigMap
#  shell: kubectl patch configmap nginx-configuration -n ingress-nginx --patch "$(cat /tmp/patch.yml)"
#  ignore_errors: True
#  become: yes
